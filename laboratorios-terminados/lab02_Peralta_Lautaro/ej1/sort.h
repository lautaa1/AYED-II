#ifndef _SORT_H
#define _SORT_H

#include <stdbool.h>

void insertion_sort(int a[], unsigned int length);
/*
    Sort the array 'a' using the Insertion sort algorithm. The resulting sort
    will be ascending according to the goes_before funtion.
    Ordena el array 'a' usando el algoritmo de ordenamiento por inserción. El resultado de la ordenación
    será ascendente según la función goes_before.

    The array 'a' must have exactly 'length' elements.
    El array 'a' debe tener exactamente 'length' elementos.

*/

#endif
